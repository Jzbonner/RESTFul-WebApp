{
  "name": "react-backend",
  "version": "0.0.0",
  "description": "",
  "main": "index.js", 
  "author": "Jarrett Bonner",
  "private": true,
  "scripts": {
    "start": "node index.js",
    "heroku-postbuild": "cd client && npm install --only=dev && npm install && npm run build"
  },
  "dependencies": {
    "express": "~4.16.0"
  }
}
